<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appmudanza">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="abdroid.hardware.location.gps" />
    <uses-permission
        android:name="android.haedware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="QHAPAQ ÑAN"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivityRegistro"></activity>
        <activity android:name=".MainActivityLogin" />
        <activity android:name=".Camioneta.MainActivityCamionetacuatro" />
        <activity android:name=".Camioneta.MainActivityCamionetatres" />
        <activity android:name=".Camioneta.MainActivityCamionetados" />
        <activity android:name=".Camioneta.MainActivityCamionetauno" />
        <activity android:name=".MainActivityCamioneta" />
        <activity android:name=".Camion.MainActivityCamioncuatro" />
        <activity android:name=".Camion.MainActivityCamiontres" />
        <activity android:name=".Camion.MainActivityCamiondos" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Camion.MainActivityCamionuno" />
        <activity android:name=".MainActivityCamion" />
        <activity android:name=".MainActivitySplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppMudanza.NoActionBar" />
    </application>

</manifest>